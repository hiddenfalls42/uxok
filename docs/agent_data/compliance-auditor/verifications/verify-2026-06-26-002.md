# Documentation Verification Report
**Verify ID:** `verify-2026-06-26-002`
**Verifies handoff:** `audit-2026-06-26-002`
**Scope:** Writer output for the secure-capabilities feature set — 4 new pages, 5 updated pages, 2 index pages
**Skills applied:** `.claude/skills/public-document-blueprint-SKILL/SKILL.md`, `.claude/skills/writing-style-SKILL/SKILL.md`
**Verifier:** documentation-auditor-agent (verification half)
**Timestamp:** `2026-06-26T00:00:00Z`
**Verdict:** **CHANGES-REQUIRED**

---

## Per-item verdict table

| Page | Accuracy | Placement / structure | Density (cap) | Voice | Verdict |
|---|---|---|---|---|---|
| `explanation/secure-capability-access.md` (NEW) | PASS | PASS | PASS (no cap; 1599 within "as long as needed") | PASS | **PASS** |
| `how-to/how-to-lock-down-capability-access.md` (NEW) | PASS | PASS | PASS (548 ≤ 600) | PASS | **PASS** |
| `how-to/how-to-probe-a-plugin-before-admission.md` (NEW) | PASS | PASS | PASS (432 ≤ 600) | PASS | **PASS** |
| `how-to/how-to-resolve-a-live-plugin-instance.md` (NEW) | PASS | PASS | PASS (358 ≤ 600) | PASS | **PASS** |
| `explanation/capability-system.md` (UPD) | PASS | PASS (non-restatement respected) | PASS | PASS | **PASS** |
| `how-to/how-to-work-with-capabilities.md` (UPD) | **FAIL** (4 non-runnable constructors) | PASS | **FAIL** (683 > 600) | PASS | **FAIL** |
| `how-to/how-to-use-hot-reload.md` (UPD) | PASS (event payloads verified) | PASS | **FAIL** (718 > 600) | PASS | **PARTIAL** |
| `how-to/how-to-use-capability-policies.md` (UPD) | PASS | PASS | **FAIL** (657 > 600, marginal) | PASS | **PARTIAL** |
| `how-to/index.md` (UPD) | PASS | PASS (all 3 new how-tos listed) | — | — | **PASS** |
| `explanation/index.md` (UPD) | PASS | PASS (new explanation listed) | — | — | **PASS** |

---

## 1. Runnable-example accuracy (highest priority)

**All four new pages: runnable examples verified accurate against `src/uxok/`.**

- **Constructor pattern.** Every example in the four new pages uses the coreless keyword-only pattern — `def __init__(self)` + `super().__init__(name=..., ...)`. `Plugin.__init__` is keyword-only (`*` at `src/uxok/plugin/_base.py:69`); the new pages never pass `core` positionally. Correct.
- **`Core(capability_access="sealed")`.** `Core.__init__(**kwargs)` forwards to `CoreConfig(**kwargs)` (`_core.py:97,117`); `capability_access` is validated against `{"open","declared","sealed"}` (`_config_validation.py:58-62`), raising `ValueError` on an unknown value. The lock-down page's claim (step 1) is exact.
- **`get_capability` / `CapabilityAccessError`.** Gate at `_base.py:587-588` → `enforce_requires` (`_core_facet.py:52-73`) raises `CapabilityAccessError` for a name outside `requires | resolves`. `CapabilityAccessError.capability` exists (`errors.py:103`), so the lock-down page's `exc.capability` is real. Under sealed mode an **untyped string** resolution returns the raw provider (`enforce_requires` returns name, `Core.get_capability` returns provider) — the runnable example's `greeter.greet("world")` works; the `Overreacher` resolution raises and is caught. Verified runnable.
- **`check_plugin` / `AdmissionResult`.** `Core.check_plugin` is async (`_core.py:291`) and returns `AdmissionResult`. The field set (`ok`, `missing_requires`, `id_conflict`, `provides_conflicts`, `contract_failures`) was established PASS by the caller. The probe runnable example's expected outputs (`ok=True/False`, `missing_requires=frozenset()` / `frozenset({'analytics'})`) match.
- **`LifecycleFacet.get_plugin` / `kernel.lifecycle`.** `kernel.lifecycle` is intercepted in `Core.get_capability` **before** the capability-system lookup and **independent of mode** (`_core.py:913-916`), returning `LifecycleFacet`, which exposes `register_plugin`/`unregister_plugin`/`load_plugin`/`get_plugin` (`_core_facet.py:147-175`). The resolve-a-live-instance page uses default `Core()` (open mode) and still resolves the grant — verified runnable. `kernel.lifecycle` is also a `RESERVED_CAPABILITIES` entry exempt from the missing-capability admission check (`_capability_system.py:48`), so `requires={"kernel.lifecycle"}` never fails registration, as the page states.
- **`on_start` firing.** `register_plugin` calls `plugin.start()` (`_core.py:358`), which runs `on_start`. Every runnable example that relies on `on_start` side effects is therefore correct.
- **`@event` payload keys.** `core.capability.revoked` → `{capability, old_provider_id}` (`_shared_utils.py:78-79`); `core.capability.rebound` → `{capability, old_provider_id, new_provider_id}` (`_core.py:766-771`); `core.plugin_reloaded` → `{plugin_name, old_id, new_id}` (`_core.py:573-575`); `core.plugin_error` carries `plugin_name`/`phase`/`error_type`/`error` (`_base.py:611-617`, `_core.py:381-383`). Every key used in the hot-reload page is exact.
- **Exception names.** `CapabilityAccessError`, `StalePluginError`, `CapabilityError`, `MissingCapabilityError`, `PluginError` are all real top-level exports (`uxok/__init__.py:14-27`).

**One non-blocking accuracy note (Info):** `how-to-lock-down-capability-access.md` step 5 (`HealthCheck`) references `self.greeter` before it is assigned — the snippet only sets it in the `except StalePluginError` branch. This is an illustrative fragment showing the recovery pattern, not the page's labelled "Runnable example" (lines 89-126, which is fully runnable and does not use it). Acceptable as a fragment; optionally initialize `self.greeter` in `on_start` for cleanliness.

## 2. Density

How-to cap is **200–600 words** (blueprint density table). Using the caller's corrected word counts:

- `how-to-lock-down-capability-access.md` — 548 — **PASS**
- `how-to-probe-a-plugin-before-admission.md` — 432 — **PASS**
- `how-to-resolve-a-live-plugin-instance.md` — 358 — **PASS**
- `how-to-use-hot-reload.md` — 718 — **FAIL** (118 over). The mandated `core.capability.revoked` / `.rebound` subscription section (C-006) is correct and task-essential, but it pushed an already-near-ceiling page over. The page sprawls across load, reload, relative imports, two observe-event sections, and a three-part error section. Trim target: condense "Handle errors during reload" (its three sub-paragraphs are the fattest prose and partially overlap general lifecycle guidance); keep the capability-events steps.
- `how-to-work-with-capabilities.md` — 683 — **FAIL** (83 over). The new `requires` vs `resolves` step (C-002) is correct and essential. The overage is inherited bloat: sections 7 (collision/selection policies) and 8 (inspect providers) restate dedicated pages (`how-to-use-capability-policies.md`, `how-to-use-plugin-collections.md`) that they already link to. Trim target: demote sections 7-8 to one-line pointers.
- `how-to-use-capability-policies.md` — 657 — **FAIL, marginal** (57 over). The M-002 `capability_access` fourth-axis note is accurate and minimal — exactly what the handoff asked for. The overage is in the pre-existing scenario prose. Lowest-priority trim.

**Explanation density.** The blueprint sets Explanation to **"Discursive — as long as needed"** — there is **no numeric word cap** for Explanation (the ~1500-word split threshold applies to *Tutorials*, not Explanation). `explanation/secure-capability-access.md` at **1599 words is within target** — single concept, coherent, no padding. **No split or trim required. PASS.**

## 3. RFC / citation leaks

`grep -niE "rfc|§|spec 0005|proposals/"` across all 9 new/touched public pages plus the 2 indexes returns **zero matches**. The `(RFC 0001 §3.2.2: ...)` citation previously at `how-to-use-plugin-collections.md:148` is **removed** — the passage now reads plain English ("A collection is for *discovery*, not invocation — `PluginView` exposes no way to call into or hand back a live instance..."). **PASS.** (The docstring-level RFC leaks logged as I-001/I-002 in the handoff are a source-code concern, out of the writer's scope, and unchanged here.)

## 4. Placement / structure

- Each new page: exactly one sentence-case `# H1`, zero wikilinks (grep clean), correct Diátaxis home (3 task pages in `how-to/`, 1 concept page in `explanation/`).
- Index coverage: `how-to/index.md` lists all three new how-tos (lines 39-41); `explanation/index.md` lists the new explanation page (line 13).
- Cross-links: all targets resolve to real files — `how-to-register-and-manage-plugins.md`, `how-to-use-tags-for-provider-selection.md`, `how-to-extend-plugin-base.md`, `how-to-execute-hooks.md`, `plugin-architecture.md`, `architecture-overview.md`, `hook-system.md`, `event-system.md`, `framework-philosophy.md`, and all four new pages all exist on disk.
- Non-restatement rule: `capability-system.md` adds a short `requires`-runtime-role note (line 23) and **names + links** `secure-capability-access.md` for the full model rather than restating the per-mode "why." Correct — no cross-layer rationale duplication. **PASS.**

## 5. Regression — pre-existing constructor bug

**Confirmed, and in scope to fix.** `how-to-work-with-capabilities.md` contains four examples using the broken pattern `def __init__(self, core)` + `super().__init__(core, ...)`:

- line 11 / (constructor body) — `StoragePlugin`
- line 32 — `DataPlugin`
- lines 62-63 — `DataPlugin` (`super().__init__(core, name="data", requires={"storage"})`)
- lines 121-126 — `LocalCache` / `RemoteCache`

Because `Plugin.__init__` is keyword-only (`*`, `_base.py:69`), passing `core` positionally raises `TypeError` on instantiation — these examples are **non-runnable**. The writer's new `resolves` example (lines 44-52) uses the correct coreless pattern, so the page now contradicts itself on the single most basic API (how to construct a `Plugin`). It is in scope: the writer edited this exact page, the blueprint requires runnable examples, and the inconsistency is glaring beside the correct new block. **Must go back to the writer.**

(Peripheral, Info — not blocking: `how-to-use-capability-policies.md` uses the same positional-`core` pattern in illustrative `StoragePluginA(core)` calls, but those classes are undefined placeholders that were never copy-paste runnable. Pre-existing, untouched by this task except the line-22 note; flag for a future cleanup pass, not this loop.)

---

## What must go back to the writer

1. **`how-to-work-with-capabilities.md` — fix 4 broken constructors (CORRECTNESS, blocking).** Convert `def __init__(self, core)` + `super().__init__(core, ...)` at lines 11, 32, 62-63, 121-126 to the coreless keyword-only form: `def __init__(self)` + `super().__init__(name=..., provides=..., requires=..., tags=...)`. They raise `TypeError` on instantiation today.

2. **`how-to-work-with-capabilities.md` — trim to ≤600 words (DENSITY, blocking).** Currently 683. Demote sections 7 (collision/selection policies) and 8 (inspect providers) to one-line pointers to the dedicated pages they already link to. Keep the new `requires` vs `resolves` step.

3. **`how-to-use-hot-reload.md` — trim to ≤600 words (DENSITY, blocking).** Currently 718. Condense the three-part "Handle errors during reload" section. Keep the mandated `core.capability.revoked` / `.rebound` subscription steps (verified accurate).

4. **`how-to-use-capability-policies.md` — trim to ≤600 words (DENSITY, low priority).** Currently 657 (57 over). Light tightening of the scenario prose; keep the `capability_access` fourth-axis note intact.

Optional (non-blocking): initialize `self.greeter` in the lock-down step-5 `HealthCheck` fragment for cleanliness.

---

## Overall verdict: CHANGES-REQUIRED

The core deliverables are sound: all four new pages are accurate against source, correctly placed, single-H1, wikilink-free, citation-free, and within (or uncapped for) their density bands; the explanation at 1599 words is within "as long as needed"; the capability-event additions and the M-001 non-restatement note are accurate and conformant; and the RFC leak at `plugin-collections.md:148` is gone. Three items block approval: the four non-runnable constructors in `how-to-work-with-capabilities.md`, and the three how-to pages over the 600-word cap (hot-reload, work-with-capabilities, capability-policies). Return items 1-4 above to the writer; re-verify those four pages only.

---

*This report verifies writer output against the public blueprint and voice skills. No documentation was written or modified during this verification.*
